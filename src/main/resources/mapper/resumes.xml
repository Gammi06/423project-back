<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject1.domain.resumes.ResumesDao">

<select id="findById" resultType="site.metacoding.miniproject1.web.dto.response.resume.ResumeUpdateDto">
	SELECT r.id, r.title, u.photo, u.user_name, u.email, u.phone_number, r.intro FROM resumes r
	LEFT OUTER JOIN users u
	ON r.user_id = u.id
	WHERE r.id = #{id}
</select>

<select id="findByUserId" resultType="site.metacoding.miniproject1.web.dto.response.resume.ResumeListDto">
	SELECT r.id, r.title, r.created, r.status_id FROM resumes r
	WHERE r.user_id = #{id}
</select>

<delete id="deleteById">
	DELETE FROM resumes WHERE id = #{id};
</delete>






</mapper>